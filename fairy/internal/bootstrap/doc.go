// Package bootstrap owns Session Core process composition helpers.
//
// Runtime profile selection, infrastructure dependency opening, bootstrap
// status metadata, and core service wiring live here so fairy/runtime can
// remain a thin lifecycle wrapper around HTTP/SSE hubs and turn events.
package bootstrap
